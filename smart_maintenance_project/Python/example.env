AZURE_AI_PROJECT_ENDPOINT=
AZURE_AI_MODEL_DEPLOYMENT_NAME=
# Azure AI Search (RAG) — USE the same index you already created in Foundry
AZURE_SEARCH_SERVICE=
             # name only, not url
AZURE_SEARCH_API_KEY=                    # query or admin key
AZURE_SEARCH_INDEX=

# Optional: CSV path if you want last-row test; else leave blank
CSV_PATH=   